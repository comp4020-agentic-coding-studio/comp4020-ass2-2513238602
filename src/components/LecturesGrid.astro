---
import { getPublishedCollection } from "astro-course-university/content";
import Card from "astro-theme-university/components/Card.astro";
import CardGrid from "astro-theme-university/components/CardGrid.astro";
import { withBase } from "astro-theme-university/url";
import { formatCourseDate } from "../lib/dates";
import { splash } from "../lib/riot-assets";

const lectures = await getPublishedCollection("lectures");
const visible = lectures.sort((a, b) => a.data.date.getTime() - b.data.date.getTime());
const championByWeek = ["Volibear", "Riven", "Gnar", "Jax", "LeeSin", "Ornn", "Fiora", "TwistedFate", "Shen", "Malphite", "Viego", "Ryze"];
---

<CardGrid columns={2}>
  {
    visible.map((lecture) => (
      <Card headingLevel="h2" title={`W${String(lecture.data.week).padStart(2, "0")} / ${lecture.data.title}`} href={withBase(`/lectures/${lecture.id}/`)} image={splash(championByWeek[lecture.data.week - 1])} imageAlt={`${championByWeek[lecture.data.week - 1]} official splash artwork`}>
        {lecture.data.description && <p>{lecture.data.description}</p>}
        <p><small>Week {lecture.data.week} · {formatCourseDate(lecture.data.date)}</small></p>
      </Card>
    ))
  }
</CardGrid>
